import { styled } from '../../styles';

export const Card = styled('TouchableOpacity', {
  borderRadius: '$medium',
  overflow: 'hidden',
  borderWidth: 1,
  borderColor: '$backdrop',
  // flexDirection: 'row',
});
