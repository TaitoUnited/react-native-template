export type Config = {
  appIdSuffix?: string;
  iconImage: string;
  adaptiveIcon: {
    backgroundColor: string;
    foregroundImage: string;
  };
  splash: {
    backgroundColor: string;
    image: string;
  };
};
